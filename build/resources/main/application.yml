spring:
  application:
    name: StudTest

  datasource:
    url: jdbc:mysql://localhost:3306/students_db?useSSL=false&serverTimezone=UTC
    username: root
    password: adminadmin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect